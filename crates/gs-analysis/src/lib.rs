//! `gs-analysis`：通用分析引擎骨架。
//!
//! 保底 / 欧非 / 时间线聚合的具体曲线数学（`ProbabilityCurve` 的
//! `flat` / `softPity` / `progressive` 各变体，参见插件 SDK 设计文档 3.4 节）
//! 需要结合参考项目校准具体参数后再落地——鸣潮的渐进概率与米哈游的软保底
//! 数学模型不同，不能先写死一种再改。
//!
//! 本阶段先提供一个与具体游戏、具体曲线都无关的保底计数器：只负责
//! "距离上一次命中保底目标过去了几抽"，不涉及任何概率计算。

mod pity;

pub use pity::PityCounter;

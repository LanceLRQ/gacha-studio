TODO：这里放一段脱敏后的缓存/日志片段，用来验证 manifest.ts 里
credential.urlPattern 这个正则确实能从真实缓存内容里捞出凭据 URL。

参考 plugins/genshin/fixture.test.ts 的同级文件
fixtures/genshin/credential/data_2.sample 的写法：
纯文本即可，不需要是真实的二进制缓存格式，只要能证明正则命中一次
即可。记得只用假 authkey（如 FAKE_AUTHKEY_FOR_FIXTURE_ONLY），
提交前跑一遍 `pnpm gs:sanitize fixtures/__GAME_ID__/` 确认零命中。

本文件本身不参与 assertPluginFixture 的自动化流程（该流程只读取
raw_response/ 与 expected/normalized.json），纯粹是给人工 review 与
未来实现 credential 抓取逻辑时提供参照，可按需删除或重命名。

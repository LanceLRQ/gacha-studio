// 骨架阶段的占位页面：只用来证明 Vite + React + TypeScript 链路能跑通，
// 不引入 UI 库、路由或状态管理——这些留给后续里程碑决定。
export function App() {
  return (
    <main>
      <h1>Gacha Studio</h1>
      <p>工程骨架已就绪，等待 M1 接入第一个插件。</p>
    </main>
  );
}
